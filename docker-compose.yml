version: '3'
services:
  frontend:
    container_name: client
    build:
      context: ./
    ports:
      - "5000:5000"
    
  backend:
    container_name: backend
    build:
      context: ./backend
    ports:
      - "4000:4000"
    
    
